# iMoo Desktop - Python 依赖
# GUI
PySide6>=6.6.0
PySide6-Addons>=6.6.0

# 网络
requests>=2.28.0

# 配置
PyYAML>=6.0

# 图片处理（动态图片上传：Pillow 转 WebP）
Pillow>=10.0.0

# 打包（Nuitka 替代 PyInstaller）
# Nuitka 编译成 C 再编译成二进制，比 PyInstaller 性能更好、更难反编译
nuitka>=2.0.0
# Nuitka 依赖：C 编译器（gcc/clang/MSVC），系统级安装，不通过 pip
